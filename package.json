{
  "name": "reactive2016",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "config": {
    "build": "--output-file slides.html --style slides.css --template template.html slides.md"
  },
  "scripts": {
    "build": "markdown-to-slides $npm_package_config_build",
    "watch": "markdown-to-slides --watch $npm_package_config_build",
    "start": "(servant -p 3002 &) && gnome-open http://localhost:3002/slides.html && npm run watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown-to-slides": "^1.0.3",
    "servant": "^4.4.0"
  }
}
